# Crie um programa que analise uma solicitação de crédito.
#Feito por Bruna
idade = int(input("Digite sua idade:"))
salario_mensal = float(input("Digite seu sálario:"))
valor_divida_atual = float(input("Digite sua divida:"))
tempo_emprego = int(input("Digite o tempo de emprego:"))
valor_solicitado = float(input("Digite o valor solicitado:"))
numero_parcelas = int(input("Digite o número de parcelas:"))

comprometimento = valor_divida_atual / salario_mensal
valor_parcela = valor_solicitado / numero_parcelas
novo_comprometimento =  comprometimento * (valor_parcela / salario_mensal)
#falta colocar o código para imprimir na tela.

#Feito por Elen
if (21<= idade <= 65 and
salario >= 2500 and
tempo_emprego >= 12 and
comprometimento_atual <= 30 and
nova_parcela <= salario * 0,25):

#Falta colocar a parte "aprovada com restrições".

#Feito por Ingrid
if idade < 21:
    print("Reprovada")
    print("Idade menor que 21 anos")

elif salario < 2500:
    print("Reprovada")
    print("Salário menor que R$ 2.500")

elif tempo_emprego < 6:
    print("Reprovada")
    print("Menos de 6 meses de emprego")

elif comprometimento > 0, 30:
    print("Reprovada")
    print("Dívida muito alta")

else:
    print("Aprovada")

#Calculo completo de imposto de renda










# sistema de avaliação academica
nota1 = float(input('digite a nota:'))
nota2 = float(input('digite a nota:'))
nota3 = float(input('digite a nota:'))
frequencia = float(input('digite a frequencia:'))
atividade_entregues = int(input('quantas atividades foram entregues?'))
total_atividades = int(input('qual o tatal das atividades?'))

media = (nota1 + nota2 + nota3)/3
percentual_entregue = (atividade_entregues / total_atividades) * 100

print('media:',media,)
print('percentual de atividades entregues:', percentual_entregue,'%')

if media >= 9 and frequencia >= 90 and atividade_entregues == total_atividades:
    print('aprovada com excelencia')

elif media >= 7 and frequencia >= 75 and percentual_entregue >= 70:
    print('aprovada')

elif media >= 5 and media < 7 and frequencia >= 75:
    print('recuperação')
    print('requisito que impediu a aprovação: media de', media,'e frequencia de',frequencia,'%')

elif frequencia < 75:
    print('reprovado por frequencia')
    print('requisito que impediu a aprovação: frequencia de',frequencia,'%')

else:
    print('reprovado por nota')


    #Cálculo de frete inteligente
peso = float(input('digite o peso da encomenda:'))
distancia = float(input('digite a distancia em Km:'))

print('Tipo de entrega: 1(normal), 2(expressa), 3(urgente)')
tipo_entrega = int(input('qual o tipo de entrega:'))

print('assinatura: 1(sim), 2(não)')
assinante = int(input('possui assinatura:'))

valor_compra = float(input('qual o valor da compra:'))

adicional_tipo = 0
adicional_peso = 0
adicional_distancia = 0
frete_total = 0
desconto_porcentagem = 0
frete_porcentagem = 0

taxa_peso = peso * 2.50
taxa_distancia = distancia * 0.30


print('taxa por peso:', taxa_peso)
print('taxa por distancia:', taxa_distancia)


if tipo_entrega == 1:
	adicional_tipo = 0

elif tipo_entrega == 2:
	adicional_tipo = (taxa_peso + taxa_distancia) * 0.30

elif tipo_entrega == 3:
	adicional_tipo = (taxa_peso + taxa_distancia) * 0.60

if peso > 30:
	adicional_peso = 80

if distancia > 500:
	adicional_distancia = 100

frete_total = (taxa_peso + taxa_distancia + adicional_tipo + adicional_peso + adicional_distancia)

if assinante == 1:
	desconto_porcentagem += 0.15

if valor_compra > 1000:
	desconto_porcentagem += 0.10

if desconto_porcentagem > 0.20:
	desconto_porcentagem = 0.20

    # falta terminar 